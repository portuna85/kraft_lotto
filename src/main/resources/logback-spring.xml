<?xml version="1.0" encoding="UTF-8"?>
<!--
    Logback 설정: 레벨별 파일 분리 + 콘솔 동시 출력
    - 위치: ./logs/
    - 파일명: lotto-{LEVEL}.log (현재) / lotto-{LEVEL}.YYYY-MM-DD.N.log.gz (롤링)
    - 정책: 일자별 + 파일당 10MB 초과 시 분할, 30일 보관, 총합 1GB 상한
    - LevelFilter 로 각 레벨 단독 파일에 해당 레벨만 기록
    - Spring Boot 의 기본 변수 (CONSOLE_LOG_PATTERN, FILE_LOG_PATTERN) 활용
-->
<configuration scan="true" scanPeriod="30 seconds">

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>
    <property name="APP_NAME" value="${APP_NAME:-lotto}"/>
    <property name="LOG_PATTERN_FILE"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{40} - %msg%n"/>
    <!-- 레벨별 파일의 공통 롤링 정책(중복 제거) -->
    <property name="LEVEL_FILE_MAX_SIZE" value="10MB"/>
    <property name="LEVEL_FILE_MAX_HISTORY" value="30"/>
    <property name="LEVEL_FILE_TOTAL_CAP" value="1GB"/>
    <!-- 통합(ALL) 파일은 다른 정책: 더 빠른 회전, 짧은 보관 -->
    <property name="ALL_FILE_MAX_SIZE" value="20MB"/>
    <property name="ALL_FILE_MAX_HISTORY" value="14"/>
    <property name="ALL_FILE_TOTAL_CAP" value="2GB"/>

    <!-- 레벨별 단독 파일 Appender 생성 매크로(중복 정의로 정적 작성) -->
    <appender name="FILE_TRACE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-trace.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>TRACE</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-trace.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LEVEL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LEVEL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LEVEL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="FILE_DEBUG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-debug.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-debug.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LEVEL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LEVEL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LEVEL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="FILE_INFO" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-info.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-info.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LEVEL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LEVEL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LEVEL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="FILE_WARN" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-warn.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>WARN</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-warn.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LEVEL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LEVEL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LEVEL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${LEVEL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${LEVEL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LEVEL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 통합 로그(전 레벨 포함) - 디버깅 편의를 위해 별도 보관 -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-all.log</file>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archive/${APP_NAME}-all.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${ALL_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${ALL_FILE_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${ALL_FILE_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 개발 프로파일: 콘솔 + 파일 모두 (DEBUG 까지) -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE_TRACE"/>
            <appender-ref ref="FILE_DEBUG"/>
            <appender-ref ref="FILE_INFO"/>
            <appender-ref ref="FILE_WARN"/>
            <appender-ref ref="FILE_ERROR"/>
            <appender-ref ref="FILE_ALL"/>
        </root>
        <logger name="com.lotto" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE_TRACE"/>
            <appender-ref ref="FILE_DEBUG"/>
            <appender-ref ref="FILE_INFO"/>
            <appender-ref ref="FILE_WARN"/>
            <appender-ref ref="FILE_ERROR"/>
            <appender-ref ref="FILE_ALL"/>
        </logger>
    </springProfile>

    <!-- 운영 프로파일: 콘솔 + 파일 (INFO 이상) -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE_INFO"/>
            <appender-ref ref="FILE_WARN"/>
            <appender-ref ref="FILE_ERROR"/>
            <appender-ref ref="FILE_ALL"/>
        </root>
    </springProfile>

    <!-- 기본(프로파일 없음 또는 test): dev 와 동일하게 동작 -->
    <springProfile name="!dev &amp; !prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE_DEBUG"/>
            <appender-ref ref="FILE_INFO"/>
            <appender-ref ref="FILE_WARN"/>
            <appender-ref ref="FILE_ERROR"/>
            <appender-ref ref="FILE_ALL"/>
        </root>
        <logger name="com.lotto" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE_DEBUG"/>
            <appender-ref ref="FILE_INFO"/>
            <appender-ref ref="FILE_WARN"/>
            <appender-ref ref="FILE_ERROR"/>
            <appender-ref ref="FILE_ALL"/>
        </logger>
    </springProfile>

</configuration>

